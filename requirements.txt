# IMPORTANT: install torch FIRST from the CUDA 12.8 index if you have an
# RTX 50-series (Blackwell / sm_120) GPU, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# Then install the rest of this file. See README.md for details.

opensr-model
opensr-utils
rasterio
omegaconf
streamlit
numpy
scikit-image
matplotlib
Pillow
requests
python-dotenv

huggingface_hub>=0.24.0
